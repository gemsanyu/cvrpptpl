#Installation

```
pip install -r requirements.txt
```


#Generating Problem Instances

Example:
```
python generate_random_instance.py --customer-location-mode c --num-customers 20 --demand-generation-mode sl
```

To understand all arguments, do:

```
python generate_random_instance.py --help
```